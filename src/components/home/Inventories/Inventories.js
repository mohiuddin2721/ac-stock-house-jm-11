import React from 'react';
import './Inventories.css';

const Inventories = () => {
    return (
        <div>
            <h3>All Inventories items</h3>
        </div>
    );
};

export default Inventories;